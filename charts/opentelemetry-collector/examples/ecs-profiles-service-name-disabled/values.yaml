mode: daemonset

presets:
  profilesCollection:
    enabled: true
  profilesK8sAttributes:
    enabled: false
  ecsAttributesContainerLogs:
    enabled: true

config:
  service:
    telemetry:
      logs:
        level: info
    pipelines:
      profiles:
        receivers:
          - otlp
        processors:
          - memory_limiter
        exporters:
          - debug
